# CrewUp AI Netlify Preview

이 폴더는 Netlify 수동 배포용 정적 사이트입니다.

## 배포 방법

1. Netlify 접속
2. Add new site → Deploy manually
3. 이 폴더(`crewup_netlify`)를 드래그 앤 드롭
4. 생성된 URL을 모바일에서 열기

## 포함 파일

- `index.html`: CrewUp AI 정적 웹 프로토타입
- `README.md`: 배포 안내

현재 파일은 백엔드/Supabase 연결이 없는 정적 HTML 시안입니다.
